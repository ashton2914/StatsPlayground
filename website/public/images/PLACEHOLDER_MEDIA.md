# Production media required

GitHub Pages deployment is intentionally blocked while this file exists.

Replace `statsplayground-workspace.webp` and `statsplayground-analysis.webp`
with current, privacy-reviewed product screenshots. Preserve the tested minimum
dimensions, run `npm --prefix website run test`, review both desktop and mobile
captures, then delete this marker to enable deployment.